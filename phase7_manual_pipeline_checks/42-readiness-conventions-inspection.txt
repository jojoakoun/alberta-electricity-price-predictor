===== PROJECT STATUS DOCUMENT =====
# Project Status

_Last updated: July 2026_

---

# Project Overview

## Project

**WattWise — Alberta Electricity Price Predictor**

WattWise is a production-oriented machine learning application that forecasts hourly Alberta electricity pool prices, estimates spike risk, and converts those forecasts into operational electricity-use recommendations.

The system includes:

- historical and live AESO data ingestion;
- data quality validation;
- feature engineering;
- multi-horizon regression;
- spike-risk classification;
- controlled model lifecycle management;
- secure model release delivery;
- an hourly prediction worker;
- PostgreSQL persistence;
- a Node.js and Express REST API;
- a bilingual React frontend;
- Railway deployment configuration.

---

# Current Development Status

## Current Milestone

**Phase 6 — API, Frontend, and Production Readiness**

Status:

**Implemented and verified locally; Railway deployment evidence remains
pending**

No final remediation commit has been designated. The current evidence belongs
to the uncommitted working tree on:

```text
feature/phase-6-production-readiness
```

---

## Current Repository Status

- ✅ Project foundation complete
- ✅ Data engineering complete
- ✅ Feature engineering complete
- ✅ Regression modeling complete
- ✅ Spike classification complete
- ✅ Protected test evaluation complete
- ✅ PostgreSQL application foundation complete
- ✅ Operational prediction worker complete
- ✅ Dynamic decision layer complete
- ✅ Express REST API complete
- ✅ React frontend complete
- ✅ English and French interfaces complete
- ✅ Production web-serving implementation complete
- ✅ Model lifecycle foundation complete
- ✅ Active-model registry complete
- ✅ Model release installer complete
- ✅ Manual promotion and rollback complete
- ✅ Railway web configuration complete
- ✅ Railway worker configuration complete
- ✅ Canonical automated local verification complete
- ✅ Production Express/API/SPA smoke test complete locally
- ✅ Configured-PostgreSQL worker idempotence and backfill checks complete
- ✅ Local Python 3.14 `wattwise-worker` console command verified
- ⏳ First Railway deployment pending
- ⏳ Cloud production evidence pending
- ⏳ Durable cloud lifecycle storage deferred

---

# Development Progress

| Phase | Status |
|---|---|
| Phase 0 — Project Foundation | ✅ Complete |
| Phase 1 — Data Engineering | ✅ Complete |
| Phase 2 — Feature Engineering | ✅ Complete |
| Phase 3 — Regression Modeling | ✅ Complete |
| Phase 4 — Classification and Spike Risk | ✅ Complete |
| Phase 5 — Application Foundation | ✅ Complete |
| Phase 6 — API and Frontend | ✅ Implemented and verified locally |
| Production Deployment | ⏳ Pending Railway verification |

---

# System Architecture

```text
One-time bootstrap
Historical CSV -> make seed-history -> PostgreSQL hourly_prices

Recurring operational cycle
PostgreSQL latest hour -> bounded AESO overlap -> normalize and upsert
-> PostgreSQL inference window -> five predictions -> PostgreSQL persistence
-> Express REST API -> React web application

Research rebuild (separate entry point)
Historical CSV + AESO API -> interim/processed datasets -> benchmark reports
```

PostgreSQL is the steady-state operational source of truth. The historical CSV
is a one-time bootstrap and research input, not an hourly Railway dependency.

The model lifecycle is isolated from hourly production inference:

```text
Historical Dataset
        │
        ▼
Deterministic Expanding Split
        │
        ▼
Isolated Challenger Training
        │
        ▼
Champion–Challenger Comparison
        │
        ▼
Manual Promotion Review
        │
        ▼
Release Bundle
        │
        ▼
Active Model Registry
```

---

# Architecture Status

## Data Engineering

Completed capabilities:

- one-time historical CSV seeding into PostgreSQL;
- bounded AESO Pool Price API synchronization derived from the latest
  PostgreSQL timestamp;
- API response normalization;
- UTC and Alberta-local timestamp handling;
- explicit merge precedence: API data may fill a null actual price, while a
  valid finalized actual price is preserved and the latest API forecast revision
  wins;
- hourly duplicate removal;
- schema validation;
- inference-window continuity validation without actual-price imputation;
- explicit candidate and previous-hour `forecast_price` validation without
  fallback, interpolation, forward fill, or substitution;
- data-quality reporting;
- separate research rebuild, one-time seed, and recurring operational refresh
  entry points;
- PostgreSQL-first feature preparation for serving.

The recurring worker does not require pre-existing files under:

```text
data/raw/*.csv
data/interim/*.csv
data/processed/*.csv
```

---

## Feature Engineering

Completed feature groups:

- calendar features;
- hour-of-day features;
- day-of-week features;
- seasonal features;
- lagged prices;
- rolling price statistics;
- AESO forecast-price features;
- Alberta Internal Load features;
- horizon-specific regression targets;
- horizon-specific classification targets.

Shared model feature definitions are centralized in:

```text
src/electricity_predictor/features/feature_columns.py
```

This shared definition is used by:

- model training;
- final evaluation;
- model serving;
- the application worker.

---

# Machine Learning Status

## Forecast Horizons

The system produces forecasts for:

- 1 hour;
- 3 hours;
- 6 hours;
- 12 hours;
- 24 hours.

A successful production prediction run must contain exactly:

```text
[1, 3, 6, 12, 24]
```

---

## Evaluation Protocol

The project uses a fully chronological evaluation workflow.

Safeguards include:

- fixed benchmark calendar periods;
- chronological train, validation, and protected test splits;
- a 24-hour purge between partitions;
- `TimeSeriesSplit` with `gap=24`;
- train-only spike-threshold estimation;
- no random train/test splitting;
- no protected-test use during model selection;
- no future information leakage;
- isolated lifecycle challenger training;
- manual model promotion.

The lifecycle expanding-split configuration includes:

```text
Split strategy:             expanding
Validation window:          365 days
Protected test window:      180 days
Purge duration:             24 hours
Minimum training history:   1,095 days
Retraining interval:        90 days
Promotion policy:           manual
Automatic promotion:        disabled
```

---

# Regression Models

## Active Served Regression Lineage

The active regression version in
`models/production/active_models.json` is:

```text
candidate-expanding-20260719T130000-dd356b9313f2
```

The served model is selected independently for each horizon:

| Horizon | Active model key | Serving meaning |
|---:|---|---|
| 1 h | `lasso_regression_tuned` | learned regression forecast |
| 3 h | `random_forest_regressor_tuned` | learned regression forecast |
| 6 h | `lasso_regression_tuned` | learned regression forecast |
| 12 h | `lasso_regression_tuned` | learned regression forecast |
| 24 h | `naive_baseline` | persistence/reference forecast from `actual_price_lag_1h` |

The +24 h point remains one of the five authentic forecasts, but its persistence
identity is disclosed. It is never eligible to create a best-time or savings
claim.

## Implemented Model Families

- Naive Baseline
- Linear Regression
- Ridge Regression
- Lasso Regression
- Elastic Net
- Random Forest Regression

---

## Fixed Historical Benchmark Selection

The following tables come from the fixed-calendar benchmark reports under
`reports/`. They remain reproducible historical evidence; they are not a list
of mutable production registry entries.

| Horizon | Selected Model | Validation MAE | Validation RMSE |
|---:|---|---:|---:|
| 1 h | Lasso Regression | 33.38 | 80.30 |
| 3 h | Random Forest Regression | 46.79 | 98.54 |
| 6 h | Lasso Regression | 55.64 | 110.04 |
| 12 h | Lasso Regression | 59.86 | 114.33 |
| 24 h | Naive Baseline | 55.73 | 136.67 |

---

## Fixed Historical Protected Regression Test Results

| Horizon | Model | Test MAE | Test RMSE |
|---:|---|---:|---:|
| 1 h | Lasso Regression | 26.56 | 80.36 |
| 3 h | Random Forest Regression | 42.42 | 96.53 |
| 6 h | Lasso Regression | 44.40 | 100.31 |
| 12 h | Lasso Regression | 48.11 | 101.48 |
| 24 h | Naive Baseline | 42.71 | 127.93 |

The historical benchmark and the active served lineage both select the naive
baseline at +24 h.

---

# Classification Models

## Active Served Classification Lineage

Classification remains intentionally active as `legacy-unversioned`, sourced
from the legacy artifacts referenced by the active registry. The latest
classification challenger did not pass its complete promotion gate and was not
promoted.

The active champion uses a train-derived spike-price threshold of
`170.77 $/MWh`. The rejected challenger derived `157.885 $/MWh`; that observed
train-derived threshold change did not alter the active threshold.

## Implemented Model Families

- Naive Spike Baseline
- Logistic Regression
- Random Forest Classification
- Gradient Boosting Classification

---

## Fixed Historical Benchmark Selection

| Horizon | Selected Model |
|---:|---|
| 1 h | Random Forest |
| 3 h | Random Forest |
| 6 h | Random Forest |
| 12 h | Random Forest |
| 24 h | Random Forest |

---

## Fixed Historical Validation Classification Results

| Horizon | Accuracy | Precision | Recall | F1 | PR-AUC |
|---:|---:|---:|---:|---:|---:|
| 1 h | 0.951 | 0.669 | 0.545 | 0.601 | 0.645 |
| 3 h | 0.932 | 0.500 | 0.505 | 0.502 | 0.492 |
| 6 h | 0.923 | 0.427 | 0.371 | 0.397 | 0.358 |
| 12 h | 0.897 | 0.315 | 0.435 | 0.365 | 0.286 |
| 24 h | 0.916 | 0.372 | 0.334 | 0.352 | 0.286 |

---

## Fixed Historical Protected Classification Test Results

| Horizon | Threshold | Precision | Recall | F1 | PR-AUC |
|---:|---:|---:|---:|---:|---:|
| 1 h | 0.45 | 0.2062 | 0.3822 | 0.2679 | 0.3055 |
| 3 h | 0.45 | 0.2401 | 0.2225 | 0.2310 | 0.1739 |
| 6 h | 0.45 | 0.1691 | 0.1492 | 0.1586 | 0.1016 |
| 12 h | 0.45 | 0.1077 | 0.1361 | 0.1202 | 0.0716 |
| 24 h | 0.50 | 0.1810 | 0.0995 | 0.1284 | 0.0955 |

Classification performance is materially weaker than regression performance, especially at longer horizons.

The active classification champion remains unchanged when a challenger does not pass the complete promotion gate.

---

# Spike-Risk Definition

The spike threshold is calculated from training data only.

The currently active production threshold is approximately:

```text
170.77 $/MWh
```

The project also evaluates spike behavior across time because Alberta electricity-price regimes are not stationary.

Observed spike frequency has varied materially between years.

A newly trained classification challenger calculates its own train-only
threshold, but that threshold is not activated unless the challenger passes its
gate and is manually promoted. The latest candidate threshold was
`157.885 $/MWh`; classification stayed `legacy-unversioned` at `170.77 $/MWh`.

---

# Report and Artifact Scope

| Location | Meaning |
|---|---|
| `reports/*.csv` | Fixed historical benchmark and research reports; not active-registry evidence |
| `models/candidates/candidate-expanding-20260719T130000-dd356b9313f2/reports/` | Latest lifecycle candidate evaluation and comparison evidence |
| `models/candidates/candidate-expanding-20260719T130000-dd356b9313f2/regression/` | Active promoted regression metadata and artifacts |
| `models/classification/` | Active legacy classification artifacts |
| `models/production/active_models.json` | Authoritative serving registry |

Regression and classification have separate lineages. The regression candidate
was promoted after its component gate passed; the classification challenger
failed its gate and remained inactive. A combined lifecycle

===== PROJECT DECISIONS DOCUMENT =====
# Architecture Decisions

_Last updated: July 2026_

This document records the major technical, machine-learning, product, application, lifecycle, and production decisions made throughout the development of WattWise — Alberta Electricity Price Predictor.

Each decision records:

- status;
- context;
- decision;
- consequences.

---

# Decision Status Definitions

- **Accepted** — currently active.
- **Superseded** — replaced by a newer decision.
- **Deferred** — intentionally postponed.
- **Rejected** — evaluated and not adopted.

---

# Phase 0 — Project Foundation

## P0-D01 — Python as the Primary Machine-Learning Language

### Status

Accepted

### Context

The project requires mature libraries for:

- data engineering;
- time-series feature engineering;
- regression;
- classification;
- model evaluation;
- artifact persistence;
- automated testing.

### Decision

Use Python as the primary language for:

- data ingestion;
- feature engineering;
- model training;
- model lifecycle management;
- inference;
- the operational worker.

### Consequences

Benefits:

- mature machine-learning ecosystem;
- pandas and NumPy support;
- scikit-learn integration;
- strong testing support;
- reusable training and serving code.

Trade-offs:

- lower raw execution speed than compiled languages;
- Python runtime compatibility must be managed explicitly.

---

## P0-D02 — Repository-Oriented Application Architecture

### Status

Accepted

### Context

The project is not only a model-training experiment.

It must include:

- data pipelines;
- model training;
- production inference;
- persistence;
- an API;
- a frontend;
- production configuration;
- verification tools.

### Decision

Organize the repository as one complete machine-learning application.

Primary areas:

```text
app/
configs/
data/
docs/
models/
reports/
scripts/
src/
tests/
```

### Consequences

Benefits:

- one source repository for the complete system;
- easier end-to-end testing;
- shared contracts across training and serving;
- traceable architecture decisions.

Trade-offs:

- repository complexity is higher than a notebook-only project.

---

# Phase 1 — Data Engineering

## P1-D01 — Historical Dataset Strategy

### Status

Accepted

### Context

Model training requires several years of hourly Alberta electricity data.

The AESO API alone is not treated as the only historical source.

### Decision

Use a validated historical CSV as the authoritative historical foundation.

Use the AESO Pool Price API to retrieve and append newer observations.

### Consequences

Benefits:

- reproducible historical training data;
- deterministic backfills;
- resilience when recent API access is temporarily unavailable;
- straightforward local development.

Trade-offs:

- the historical source file must occasionally be refreshed;
- historical and live sources require normalization and merge rules.

---

## P1-D02 — PostgreSQL as the Operational Database

### Status

Accepted

### Context

The application requires persistent storage for:

- historical hourly observations;
- prediction runs;
- horizon-specific forecasts;
- later observed prices;
- recommendation outputs.

### Decision

Use PostgreSQL as the operational production database.

### Consequences

Benefits:

- relational integrity;
- transactional persistence;
- migration support;
- strong Node.js and Python integration;
- production deployment compatibility.

Trade-offs:

- database provisioning and migrations are required;
- local and cloud connection configuration must be maintained.

---

## P1-D03 — UTC as the Storage-Time Standard

### Status

Accepted

### Context

Alberta uses daylight-saving time.

Storing only local timestamps can create:

- duplicate clock hours;
- missing clock hours;
- ambiguous ordering.

### Decision

Use UTC timestamps as the authoritative storage and model-ordering standard.

Preserve or derive Alberta-local time only when required for:

- feature engineering;
- presentation;
- consumer wording.

### Consequences

Benefits:

- unambiguous chronological ordering;
- correct hourly deduplication;
- safer database keys;
- consistent model targets.

Trade-offs:

- Alberta-local conversion is required at the application boundary.

---

## P1-D04 — Idempotent Hourly Data Synchronization

### Status

Accepted

### Context

The data pipeline may be executed repeatedly for the same observation window.

Repeated runs must not duplicate hourly observations.

### Decision

Use the UTC hourly timestamp as the natural key for operational price observations.

Synchronize observations through upsert behavior.

### Consequences

Benefits:

- safe retries;
- deterministic refreshes;
- no duplicate hourly rows;
- simpler scheduled operation.

Trade-offs:

- source precedence and update behavior must remain explicit.

---

# Phase 2 — Feature Engineering

## P2-D01 — Time-Series Feature Strategy

### Status

Accepted

### Context

Electricity prices exhibit:

- temporal dependence;
- daily patterns;
- weekly patterns;
- seasonal behavior;
- volatility regimes.

### Decision

Generate features including:

- calendar features;
- lag features;
- rolling statistics;
- AESO forecast features;
- Alberta Internal Load features.

### Consequences

Benefits:

- improved predictive signal;
- reusable modeling dataset;
- support for several forecast horizons.

Trade-offs:

- additional preprocessing;
- rolling and lag features require careful leakage controls.

---

## P2-D02 — Shared Feature Definitions

### Status

Accepted

### Context

Regression, classification, lifecycle training, serving, and the worker must use identical model input columns.

Duplicated feature lists can cause training-serving skew.

### Decision

Centralize model feature definitions in:

```text
src/electricity_predictor/features/feature_columns.py
```

### Consequences

Benefits:

- single source of truth;
- reduced training-serving mismatch;
- easier feature review;
- simpler serving validation.

Trade-offs:

- all model changes must respect the centralized contract.

---

## P2-D03 — Horizon-Specific Targets

### Status

Accepted

### Context

Forecasting 1 hour ahead and 24 hours ahead are materially different prediction tasks.

One shared target does not represent all horizons.

### Decision

Create separate target columns for:

```text
1 h
3 h
6 h
12 h
24 h
```

Apply the same horizon contract to regression and classification.

### Consequences

Benefits:

- horizon-specific evaluation;
- horizon-specific model selection;
- independent operational outputs.

Trade-offs:

- more models and artifacts must be maintained.

---

# Phase 3 — Regression Modeling

## P3-D01 — Separate Regression and Classification Pipelines

### Status

Accepted

### Context

Predicting a future price and predicting spike risk are different statistical problems.

### Decision

Train and evaluate separate:

- regression pipelines;
- classification pipelines.

### Consequences

Benefits:

- independent objectives;
- independent metrics;
- independent model selection;
- component-specific promotion.

Trade-offs:

===== USEFUL MAKEFILE =====
SHELL := /bin/bash
.DEFAULT_GOAL := help

PYTHON ?= .venv/bin/python
PYTEST ?= .venv/bin/pytest
MAIN_MAKEFILE ?= Makefile

.PHONY: \
	help \
	verify \
	historical-data \
	modeling-data \
	database-history \
	application

help:
	@echo ""
	@echo "WattWise — validated daily commands"
	@echo ""
	@echo "  make -f Makefile.useful verify"
	@echo "      Validate configuration, Python, API, frontend, lint, and build."
	@echo ""
	@echo "  make -f Makefile.useful historical-data"
	@echo "      Refresh clean AESO history and validate its data quality."
	@echo ""
	@echo "  make -f Makefile.useful modeling-data"
	@echo "      Rebuild clean history, features, and the training dataset."
	@echo ""
	@echo "  make -f Makefile.useful database-history"
	@echo "      Refresh clean history and synchronize hourly_prices in PostgreSQL."
	@echo ""
	@echo "  make -f Makefile.useful application"
	@echo "      Start Express and React locally; press Ctrl+C to stop both."
	@echo ""
	@echo "  Modeling and decision-policy commands are intentionally withheld"
	@echo "  until the Data Science readiness audit is fully validated."
	@echo "  Protected-test commands remain separate from research workflows."
	@echo "  research-rebuild-all is excluded because it opens protected tests."
	@echo ""

verify:
	$(MAKE) -f $(MAIN_MAKEFILE) verify \
		PYTHON=$(PYTHON) \
		PYTEST=$(PYTEST)

historical-data:
	$(MAKE) -f $(MAIN_MAKEFILE) refresh-data \
		PYTHON=$(PYTHON)
	$(MAKE) -f $(MAIN_MAKEFILE) data-quality \
		PYTHON=$(PYTHON)

modeling-data:
	$(MAKE) -f Makefile.useful historical-data \
		PYTHON=$(PYTHON) \
		PYTEST=$(PYTEST)
	$(MAKE) -f $(MAIN_MAKEFILE) features \
		PYTHON=$(PYTHON)
	$(MAKE) -f $(MAIN_MAKEFILE) feature-quality \
		PYTHON=$(PYTHON)
	$(MAKE) -f $(MAIN_MAKEFILE) training-data \
		PYTHON=$(PYTHON)

database-history:
	$(MAKE) -f Makefile.useful historical-data \
		PYTHON=$(PYTHON) \
		PYTEST=$(PYTEST)
	$(MAKE) -f $(MAIN_MAKEFILE) sync-history \
		PYTHON=$(PYTHON)

application:
	$(MAKE) -f $(MAIN_MAKEFILE) dev

===== PROJECT EXPORT CHECKER ENTRYPOINT =====
#!/usr/bin/env python3

"""Verify the ignored WattWise project export without extracting it."""

from __future__ import annotations

from pathlib import Path

from project_export.verification import verify_project_export


ROOT = Path(__file__).resolve().parents[1]
EXPORT_DIR = ROOT / "context_exports"
ZIP_PATH = EXPORT_DIR / "alberta-electricity-price-predictor.zip"
MANIFEST_PATH = EXPORT_DIR / "project_files_manifest.txt"
CONTEXT_PATH = EXPORT_DIR / "project_context_full.txt"
EXCLUDED_PATH = EXPORT_DIR / "project_excluded_manifest.txt"


def main() -> None:
  """Verify the configured export outputs and print the stable summary."""
  verified_count = verify_project_export(
    zip_path=ZIP_PATH,
    manifest_path=MANIFEST_PATH,
    context_path=CONTEXT_PATH,
    excluded_path=EXCLUDED_PATH,
  )

  print(f"Verified files: {verified_count}")
  print("Missing manifest files: 0")
  print("Unexpected ZIP files: 0")
  print("Checksum mismatches: 0")
  print("Forbidden exported paths: 0")
  print("Project export verification passed.")


if __name__ == "__main__":
  main()

===== PROJECT EXPORT VERIFICATION MODULE =====
"""Verify project-export manifests and ZIP payloads."""

from __future__ import annotations

import csv
import hashlib
import zipfile
from pathlib import Path

from .policy import validate_archive_path


def sha256_bytes(content: bytes) -> str:
  return hashlib.sha256(content).hexdigest()


def load_manifest(manifest_path: Path) -> dict[str, dict[str, str]]:
  """Load a tab-separated manifest and reject duplicate paths."""
  with manifest_path.open("r", encoding="utf-8", newline="") as manifest_file:
    rows = list(csv.DictReader(manifest_file, delimiter="\t"))

  manifest = {row["path"]: row for row in rows}

  if len(manifest) != len(rows):
    raise ValueError("Duplicate paths exist in the manifest.")

  return manifest


def verify_project_export(
  zip_path: Path,
  manifest_path: Path,
  context_path: Path,
  excluded_path: Path,
) -> int:
  """Verify required outputs, safe paths, sizes, and manifest hashes."""
  for path in (zip_path, manifest_path, context_path, excluded_path):
    if not path.is_file() or path.stat().st_size == 0:
      raise FileNotFoundError(f"Missing or empty export file: {path}")

  manifest = load_manifest(manifest_path)

  with zipfile.ZipFile(zip_path, "r") as archive:
    corrupt_member = archive.testzip()

    if corrupt_member:
      raise ValueError(f"Corrupt ZIP member: {corrupt_member}")

    archive_members = archive.infolist()

    # Directory entries are not part of the file manifest, but their names
    # must still obey the same traversal and local-only path policy.
    for member in archive_members:
      validate_archive_path(member.filename)

    members = [member for member in archive_members if not member.is_dir()]
    paths = [member.filename for member in members]

    if len(paths) != len(set(paths)):
      raise ValueError("Duplicate paths exist in the ZIP.")

    manifest_paths = set(manifest)
    archive_paths = set(paths)
    missing = sorted(manifest_paths - archive_paths)
    unexpected = sorted(archive_paths - manifest_paths)

    if missing:
      raise ValueError(f"Files missing from ZIP: {missing}")

    if unexpected:
      raise ValueError(f"Unexpected ZIP files: {unexpected}")

    for member in members:
      row = manifest[member.filename]
      content = archive.read(member)

      if len(content) != int(row["size_bytes"]):
        raise ValueError(f"Size mismatch: {member.filename}")

      if sha256_bytes(content) != row["sha256"]:
        raise ValueError(f"Checksum mismatch: {member.filename}")

  return len(manifest)

===== SCRIPT TEST INVENTORY AND CONTENT =====
tests/scripts/__init__.py
"""Tests for repository tooling scripts."""
tests/scripts/test_project_export.py
import csv
import hashlib
from pathlib import Path
import zipfile

import pytest

from scripts.project_export.inventory import collect_project_files
from scripts.project_export.policy import (
  exclusion_reason,
  validate_archive_path,
)
from scripts.project_export.verification import (
  load_manifest,
  verify_project_export,
)
from scripts.project_export.writers import (
  write_context,
  write_manifests,
  write_zip,
)


@pytest.mark.parametrize(
  "path",
  [
    "docs/private.md",
    "reports/results.csv",
    "models/model.joblib",
    "context_exports/archive.zip",
    "codex-review.txt",
    "claude-review.md",
    "audit-final.txt",
    ".env",
    ".env.local",
    ".env.staging",
    "nested/service-account-prod.json",
  ],
)
def test_policy_excludes_local_and_secret_paths(path: str) -> None:
  assert exclusion_reason(Path(path)) is not None


def test_policy_allows_public_environment_example() -> None:
  assert exclusion_reason(Path(".env.example")) is None


def test_inventory_is_deterministic_and_excludes_local_paths(
  tmp_path: Path,
) -> None:
  (tmp_path / "zeta.txt").write_text("z", encoding="utf-8")
  (tmp_path / "alpha.txt").write_text("a", encoding="utf-8")
  (tmp_path / ".env.example").write_text("SAFE=value\n", encoding="utf-8")
  (tmp_path / ".env.private").write_text("SECRET=value\n", encoding="utf-8")
  reports = tmp_path / "reports"
  reports.mkdir()
  (reports / "result.csv").write_text("value\n1\n", encoding="utf-8")

  files, excluded = collect_project_files(tmp_path)

  assert [item.relative_path.as_posix() for item in files] == [
    ".env.example",
    "alpha.txt",
    "zeta.txt",
  ]
  assert [path for path, _ in excluded] == [".env.private", "reports/"]


def test_inventory_rejects_file_symlinks(tmp_path: Path) -> None:
  source = tmp_path / "source.txt"
  source.write_text("source", encoding="utf-8")
  link = tmp_path / "linked.txt"

  try:
    link.symlink_to(source)
  except OSError:
    pytest.skip("File symlinks are unavailable on this platform")

  files, excluded = collect_project_files(tmp_path)

  assert [item.relative_path.as_posix() for item in files] == ["source.txt"]
  assert ("linked.txt", "symbolic link") in excluded


def test_manifest_and_archive_round_trip_uses_relative_paths(
  tmp_path: Path,
) -> None:
  project_root = tmp_path / "project"
  project_root.mkdir()
  (project_root / "app.js").write_text("export const value = 1;\n", encoding="utf-8")
  files, excluded = collect_project_files(project_root)

  output = tmp_path / "output"
  manifest_path = output / "manifest.txt"
  excluded_path = output / "excluded.txt"
  context_path = output / "context.txt"
  zip_path = output / "project.zip"

  write_manifests(files, excluded, manifest_path, excluded_path)
  context_path.write_text("context\n", encoding="utf-8")
  write_zip(files, zip_path)

  assert verify_project_export(
    zip_path,
    manifest_path,
    context_path,
    excluded_path,
  ) == 1

  with zipfile.ZipFile(zip_path) as archive:
    assert archive.namelist() == ["app.js"]


def test_context_git_diagnostics_use_only_inventory_allowed_paths(
  tmp_path: Path,
  monkeypatch,
) -> None:
  project_root = tmp_path / "project"
  project_root.mkdir()
  (project_root / "app.js").write_text("safe\n", encoding="utf-8")
  (project_root / "safe*.js").write_text("also safe\n", encoding="utf-8")
  private_docs = project_root / "docs"
  private_docs.mkdir()
  (private_docs / "private.md").write_text("secret\n", encoding="utf-8")
  files, excluded = collect_project_files(project_root)

  output = tmp_path / "output"
  manifest_path = output / "manifest.txt"
  excluded_path = output / "excluded.txt"
  context_path = output / "context.txt"
  write_manifests(files, excluded, manifest_path, excluded_path)

  commands = []

  def capture_command(command, root):
    commands.append(command)
    return ""

  monkeypatch.setattr(
    "scripts.project_export.writers.run_command",
    capture_command,
  )

  write_context(
    files=files,
    excluded=excluded,
    root=project_root,
    context_path=context_path,
    manifest_path=manifest_path,
    excluded_path=excluded_path,
  )

  scoped_git_commands = [
    command
    for command in commands
    if command[:2] in (["git", "status"], ["git", "diff"])
  ]

  assert scoped_git_commands
  assert all(
    ":(top,literal)app.js" in command
    for command in scoped_git_commands
  )
  assert all(
    ":(top,literal)safe*.js" in command
    for command in scoped_git_commands
  )
  assert all("app.js" not in command for command in scoped_git_commands)
  assert all("safe*.js" not in command for command in scoped_git_commands)
  assert all("docs/private.md" not in command for command in scoped_git_commands)


@pytest.mark.parametrize("path", ["../secret", "/absolute", "docs/private.md", ".env.prod"])
def test_archive_validation_rejects_unsafe_or_forbidden_paths(path: str) -> None:
  with pytest.raises(ValueError):
    validate_archive_path(path)


def test_manifest_rejects_duplicate_paths(tmp_path: Path) -> None:
  manifest_path = tmp_path / "manifest.txt"
  manifest_path.write_text(
    "path\tsize_bytes\tsha256\tkind\n"
    "app.js\t1\ta\ttext\n"
    "app.js\t1\ta\ttext\n",
    encoding="utf-8",
  )

  with pytest.raises(ValueError, match="Duplicate paths exist in the manifest"):
    load_manifest(manifest_path)


def test_verification_rejects_unsafe_directory_only_archive_entry(
  tmp_path: Path,
) -> None:
  zip_path = tmp_path / "project.zip"
  manifest_path = tmp_path / "manifest.txt"
  context_path = tmp_path / "context.txt"
  excluded_path = tmp_path / "excluded.txt"

  with zipfile.ZipFile(zip_path, "w") as archive:
    archive.writestr("../escape/", b"")

  manifest_path.write_text(
    "path\tsize_bytes\tsha256\tkind\n",
    encoding="utf-8",
  )
  context_path.write_text("context\n", encoding="utf-8")
  excluded_path.write_text("path\treason\n", encoding="utf-8")

  with pytest.raises(ValueError, match="Unsafe archive path: ../escape/"):
    verify_project_export(
      zip_path,
      manifest_path,
      context_path,
      excluded_path,
    )


def test_verification_rejects_checksum_mismatch(tmp_path: Path) -> None:
  zip_path = tmp_path / "project.zip"
  manifest_path = tmp_path / "manifest.txt"
  context_path = tmp_path / "context.txt"
  excluded_path = tmp_path / "excluded.txt"
  content = b"actual"

  with zipfile.ZipFile(zip_path, "w") as archive:
    archive.writestr("app.js", content)

  wrong_hash = hashlib.sha256(b"different").hexdigest()
  with manifest_path.open("w", encoding="utf-8", newline="") as manifest_file:
    writer = csv.writer(manifest_file, delimiter="\t")
    writer.writerow(["path", "size_bytes", "sha256", "kind"])
    writer.writerow(["app.js", len(content), wrong_hash, "text"])

  context_path.write_text("context\n", encoding="utf-8")
  excluded_path.write_text("path\treason\n", encoding="utf-8")

  with pytest.raises(ValueError, match="Checksum mismatch: app.js"):
    verify_project_export(
      zip_path,
      manifest_path,
      context_path,
      excluded_path,
    )
